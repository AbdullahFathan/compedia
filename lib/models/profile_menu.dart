import 'package:flutter/widgets.dart';

class ProfileMenu {
  String title;
  Widget icon;
  String route;
  ProfileMenu({
    required this.title,
    required this.icon,
    required this.route,
  });
}
